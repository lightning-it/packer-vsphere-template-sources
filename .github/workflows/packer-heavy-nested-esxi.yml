# yamllint disable rule:line-length
# yamllint disable rule:truthy
---
name: Packer Heavy Nested ESXi

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize, ready_for_review]
  workflow_dispatch:
    inputs:
      build_kind:
        description: "Build kind: rhel or ubuntu"
        required: true
        default: rhel
        type: choice
        options: [rhel, ubuntu]
      build_version:
        description: "RHEL major or Ubuntu release"
        required: true
        default: "8"
        type: string

permissions:
  contents: read

jobs:
  heavy:
    name: Delegate Heavy execution
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (
        github.head_ref == 'develop' &&
        github.base_ref == 'main' &&
        github.event.pull_request.draft == false &&
        github.event.pull_request.head.repo.full_name == github.repository
      )
    uses: lightning-it/modulix-validation/.github/workflows/packer-nested-esxi-profile.yml@7733d7cbbd33c26fbac2994f2cb765c9a51645ef
    with:
      source-sha: ${{ github.event.pull_request.head.sha || github.sha }}
      build-kind: ${{ inputs.build_kind || 'rhel' }}
      build-version: ${{ inputs.build_version || '8' }}
      environment-name: packer-heavy
    secrets: inherit
